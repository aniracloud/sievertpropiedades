import { NumberpipesPipe } from './thousandsPipe';

describe('NumberpipesPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberpipesPipe();
    expect(pipe).toBeTruthy();
  });
});
